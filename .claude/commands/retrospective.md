Document lessons learned from this session into `.ai/memory/lessons.md`.

1. Review what corrections, surprises, or non-obvious things happened this session
2. Read `.ai/memory/lessons.md` to check what is already there
3. For each significant discovery, add or update a lesson:
   - Group by category: Patterns, Gotchas, Commands, Architecture, etc.
   - Format: `**Bold trigger condition**: one sentence on what to do differently`
   - 1–2 sentences max per lesson — make it actionable
4. Update existing lessons if new info adds nuance — never duplicate
5. Remove lessons that turned out to be wrong or irrelevant

Skip trivial typos and one-offs. Only capture things that would prevent future mistakes.
